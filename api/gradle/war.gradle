apply plugin: "war"

bootWar {
    mainClass = "com.kreitek.learnjhipster.LearnjhipsterApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
